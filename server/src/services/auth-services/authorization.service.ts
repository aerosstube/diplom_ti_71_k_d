import {Transaction} from "sequelize";


export class AuthorizationService {
    public static async checkUser(login: string, e_mail: string, transaction: Transaction) {
        // const user = await UserService
    }
}